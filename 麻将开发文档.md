直接答案：下面是一份面向鸿蒙（HarmonyOS）小游戏 —— 局域网联机麻将 + AI 对战的完整开发文档草案，包含需求、架构、网络协议、AI 设计、测试、里程碑与注意事项。文档可直接拿去细化或用于团队评审与排期。

我刚完成了该文档草案，包含关键设计与实现建议、示例消息格式与任务分解；接下来可以根据你的团队技术栈（ArkTS / JS / Native）和目标机型进一步细化接口实现与权限清单，或把文档导出为项目计划（Jira / GitHub Issues）并生成初始代码骨架。

开发文档 — 局域网联机麻将（HarmonyOS 小游戏） with AI
==================================================

1. 项目概述
------------
- 目标：开发一款在 HarmonyOS 平台上运行的麻将小游戏，支持局域网联机（最多 4 人）和单机/多人模式下的 AI 对战。
- 核心需求：
    - 局域网发现与配对（自动发现局域网内房间/玩家）
    - 实时对局同步（稳定、低延迟）
    - 权威服务器（host）模式避免作弊
    - 可插拔的 AI 引擎，支持多难度
    - 支持断线重连、观战（可选）、回放（可选）
- 非功能需求：
    - 延迟目标：局域网 RTT < 100ms（务实目标）
    - 最大并发玩家（本地房间）：4 人
    - 资源占用：单设备内存 < 200MB（视具体实现而定）
    - 支持主流鸿蒙设备（手机、平板、可能的电视）

2. 技术选型建议
----------------
- 前端 / 客户端框架：
    - ArkUI（ArkTS / JS）或 Quick App（若使用鸿蒙小程序形态），推荐 ArkTS（TypeScript 风格）用于长期维护和类型安全。
- 网络层：
    - LAN 发现：UDP 广播/组播 或 mDNS（Bonjour）/ DNS-SD；在鸿蒙上可使用 Socket UDP，或调用附近通信能力（若需要平台能力）。
    - 游戏通信：WebSocket over TCP（如果都在局域网）或自定义 TCP/UDP 协议；推荐使用 TCP+WebSocket 以简化可靠性和消息顺序，但对延迟敏感场景可用 UDP（需自己处理丢包/重传）。
    - 数据序列化：Protocol Buffers（高效、规范）或 JSON（开发更快）；推荐 protobuf（在手机局域网环境消息量大时更优）。
- 后端 / AI：
    - AI 在客户端本地运行（无外网依赖），使用可移植的逻辑模块（TypeScript/JavaScript 实现即可）。复杂 AI 可用 C/C++ 实现并通过 N-API/WASM 集成。
- 持久化：本地数据库（如 SQLite）或轻量存储用于历史对局、偏好设置。
- 工具：Git、CI（GitHub Actions）、自动化测试框架（单元/集成/网络仿真）。

3. 架构概览
--------------
- 模型：Host-Authoritative（主机权威）架构
    - 房主（Host）充当权威服务器：负责洗牌、发牌、执行规则判定与结算，广播游戏事件与状态快照。
    - 客户端：输入（出牌、碰、杠、和牌选择等） -> 发送动作到 Host -> Host 验证并广播结果。
- 原因：麻将涉及隐私信息（牌墙顺序），要避免作弊与状态分歧，权威模型最简单可靠。
- 备份方案：当房主断线且允许切换，可选实现 Host 迁移（选取下一个在线玩家接管并用已广播 seed/记录恢复状态），实现复杂度高，初版可不支持或限制重连/主机切换策略。

架构图（文字说明）
- 局域网发现层：广播/组播/MDNS -> 找到房间列表。
- 房间层：玩家加入 -> 建房/准备 -> 开始游戏。
- 通信层：消息总线（可靠的消息通道 WebSocket/TCP）。
- 游戏引擎层（Host）：牌堆 RNG、规则引擎、判定与结算。
- AI 层：可作为客户端替代玩家行为的模块（规则+搜索/模拟）。

4. 局域网发现与连接流程
-----------------------
两种常见可选方案（按实现复杂度与兼容性排序）：

方案 A — UDP 广播/组播（推荐）
- 房主定期（例如每 2s）通过 UDP 广播发送房间广告包（房间 ID、房主昵称、端口、房间信息、短文本说明）。
- 客户端监听广播，显示房间列表并可直接连接。
- 广播格式：小���二进制或 JSON（若使用 protobuf，定义 Discovery 消息）。
- 连接建立：客户端向房主 IP:port 发起 TCP/WebSocket 连接。

方案 B — mDNS / DNS-SD（更标准）
- 使用 mDNS 服务注册房间，客户端通过名称解析发现。
- 兼容性依赖目标设备 mDNS 支持性。

安全/权限提示：
- 需在应用清单中声明局域网访问权限与网络权限（参考鸿蒙官方权限文档）。
- 广播信息不要包含敏感信息（不包含完整牌面或种子）。

发现与连接流程序列（简述）：
1. 房主创建房间并在本机启动游戏服务器监听端口。
2. 房主开始广播房间信息（UDP 广播或 mDNS）。
3. 客户端接收到广播并显示房间列表；选择加入发送 Join 请求（TCP）。
4. Host 接受连接并将玩家加入房间，发送房间当前状态（玩家列表、座位）。
5. 所有玩家准备完成后，Host 下发游戏开始包（包含 RNG seed 或完整洗牌结果，取决于权威策略）。

5. 游戏同步策略与消息协议
-------------------------
总体原则：
- 采用事件驱动 + 权威快照。Host 接受客户端动作并广播确认（包括动作是否合法和动作导致的状态更改）。
- 定期或在关键节点发送完整快照（例如每轮、每 10 秒或关键事件后）以防 drift。

消息种类（示例）：
- Discovery（广播）
- RoomCreate/RoomJoin/RoomLeave
- Ready/Unready
- GameStart（包含 seed 或初始牌墙快照摘要）
- PlayerAction（出牌、碰、杠、吃、和、暗杠、补花等）
- ActionConfirm（Host 同意或拒绝）
- StateUpdate（局部状态变更）
- Snapshot（完整状态）
- Heartbeat / Ping
- ReconnectRequest / ReconnectState

推荐使用 protobuf 定义消息（更高效、类型清晰）。下面给出 JSON 示例（供文档/调试使用）：

```json name=example_messages.json
{
  "GameStart": {
    "roomId": "room-1234",
    "seed": 3456789123,
    "players": [
      {"seat": 0, "nickname": "A"},
      {"seat": 1, "nickname": "B"},
      {"seat": 2, "nickname": "C"},
      {"seat": 3, "nickname": "D"}
    ],
    "initialWindows": {"dealer": 0}
  },
  "PlayerAction": {
    "roomId": "room-1234",
    "playerSeat": 1,
    "actionType": "DISCARD",
    "tile": 17,
    "timestamp": 1680000000000
  },
  "ActionConfirm": {
    "roomId": "room-1234",
    "accepted": true,
    "resultingState": { "nextPlayer": 2, "lastDiscard": 17 }
  }
}
```

（上面 JSON 仅供示意；生产请用 protobuf 并为每条消息加上 sequenceId、signature optional）

消息可靠性与顺序：
- TCP/WebSocket 自带顺序与可靠性；若使用 UDP，则必须实现 sequence & ack & retransmit。
- 每条消息带 sequenceId（连续编号）和 timestamp。Host 广播包含 lastSequenceAck，以便客户端快照同步。

状态同步策略：
- 事件驱动：玩家动作 -> Host 验证 -> Host 广播动作结果。
- 快照：游戏关键状态（牌墙、手牌摘要、弃牌区、分数）以增量或全量方式同步。
- 校验：客户端可对比 Host 广播的 checksum 与本地状态以检测偏差。

抗作弊措施（局域网里相对信任，但仍需基本保护）：
- 洗牌由 Host 执行并广播 seed（seed 可与房主 ID、时间戳一起生成），在游戏开始后不可更改。
- 为防止伪造 Host，房间广播/连接可包含短期 token，连接验证。
- 可选：对关键事件做简单签名（HMAC）防篡改。

6. AI 设计与集成
-----------------
目标：支持多种难度，满足新手到高级玩家需求，并且能作为替代玩家加入局域网对战。

AI 层分两部分：
- 规则/启发式引擎（必做，快速实现）
    - 规则库包含吃碰杠胡判定、放铳判断、保留/舍弃优先级（张数、番种偏好）。
    - 难度可通过调整启发权重（例如更偏好防守或进攻）。
- 搜索/模拟引擎（增强难度）
    - Monte Carlo Simulation（蒙特卡洛模拟，MCTS 简化版本）：对每个候选出牌进行多次随机对局模拟，选择期望值最高的动作。
    - 可以采用浅层搜索 + 随机补牌模拟（因为麻将信息不完全），模拟次数和深度随难度增加。
- 训练/数据驱动（可选高级特性）
    - 用实际对局数据训练评估函数或用强化学习优化策略（需要大量数据与算力）。

AI 接口约定（伪接口）：
- init(playerSeat, knownTiles, gameRules, rngSeed)
- onStateUpdate(state)
- decideAction(timeoutMs) -> returns Action

AI 性能与多线程：
- 模拟类 AI 计算密集，建议在独立线程或异步任务中运行，避免阻塞 UI。
- 在鸿蒙上可使用 Ability 的异步线程或 WebWorker（JS 环境下）/Native 线程。

7. 游戏规则引擎（简述）
------------------------
- 支持的牌型与规则集在文档初期需明确（如国标、日本麻、广东牌规则等），建议先实现一种再逐步扩展。
- 规则项需要配置化：
    - 庄家规则（轮庄/连庄/抢杠胡等）
    - 番种、翻倍规则
    - 天胡/地胡、杠开、抢杠胡
    - 四人/三人/两人玩法差异
- 规则引擎职责：
    - 验证动作合法性
    - 判定碰/杠/胡 权利冲突的优先级（例如抢杠胡 vs 放铳）
    - 结算分数
    - 生成可视化结算条目（供 UI 展示）

8. 断线、重连与房主切换策略
---------------------------
- 断线策略：
    - 检测心跳丢失（例如 10 秒未收到心跳即为断线），Host 将玩家标记为 offline。
    - 保留玩家座位并等待重连（配置可选：保留时间例如 2 分钟）。
- 重连流程：
    - 客户端重连 -> 发送 ReconnectRequest（包含 playerId、lastSequenceId） -> Host 验证并发送最新 Snapshot。
- 房主切换（可选复杂功能）：
    - 当 Host 长时间断线且多数玩家同意时，按座位优先选举新 Host，并使用之前的 seed + 操作记录重放恢复状态；实现复杂且存在一致性风险，建议迭代实现。

9. 安全与隐私
----------------
- 局域网场景相对封闭，但仍需：
    - 不在广播中泄露私人数据。
    - 若支持云对战/账号关联，需使用 HTTPS 与 OAuth 等。
- 日志策略：仅本地保存敏感日志，并允许用户清除记录。

10. 性能与测试
----------------
- 测试场景：
    - 多设备本地真实设备互联（至少 3-4 台），覆盖不同厂商/型号。
    - 网络仿真：延迟、丢包、抖动模拟（使用工具如 tc 或移动端网络模拟器）。
    - 并发动作冲突：快速并发出牌/吃碰场景。
    - AI 性能：在弱设备上运行的模拟次数与响应时间测试。
- 性能目标：
    - 主体消息处理延迟 < 200ms（Host 处理与广播时间）
    - UI 响应 < 100ms（客户端处理）
    - 重连恢复时间 < 5s（接收 snapshot 并渲染）
- 自动化测试：
    - 单元测试（规则判定、结算）
    - 集成测试（模拟多客户端与 Host 的动作流）
    - E2E 测试：用脚本模拟完整对局

11. UI / UX 要点
-----------------
- 清晰的房间列表与状态（在线、准备、座位）
- 明确的回合提示（该谁、可操作提示、倒计时）
- 动作优先提示（“你可以碰/杠/胡”弹窗应短暂并可自定义快捷操作）
- 动画与音效：适度，不影响性能
- 可视化重连与同步提示（显示“正在等待 Host 同步...”）

12. 开发计划示例（迭代式）
---------------------------
- Phase 0（1 周）：需求确认、规则确定、技术选型、原型 UI 草图。
- Phase 1（3 周）：基础架构搭建（局域网发现、TCP/WebSocket 连接、房间管理）、简单 UI。
- Phase 2（4 周）：Host 游戏引擎（洗牌、发牌、出牌流程、基础判定）、客户端交互、基础 AI（规则式）。
- Phase 3（3 周）：消息可靠性、重连、快照、广播优化、UI 完善。
- Phase 4（4 周）：AI 增强（蒙特卡洛/模拟）、性能优化、测试与修复、上架准备。
- Phase 5（2 周）：内测、收集反馈、bug 修复、发布。

（以上为示意，具体人力与并行程度会影响周期）

13. 开发任务分解（示例）
------------------------
- 网络模块：Discovery、ConnectionManager、MessageSerializer、Heartbeat（2 人，2-3 周）
- 房间与对局管理：RoomService、SeatManagement、GameLoop（2 人，3 周）
- 游戏规则引擎：Rules、Scoring、Validator（2 人，3 周）
- AI 模块：RuleAI、MonteCarloAI、AIManager（1-2 人，3-4 周）
- UI/UX：Lobby、Room、InGame HUD、动画（2 人，4 周）
- 测试与 QA：网络仿真脚本、自动化测试（1 人，贯穿周期）

14. 示例消息定义（建议使用 protobuf，下面示意 JSON 结构）
-------------------------------------------------

```json name=message_schema_example.json
{
  "CommonHeader": {
    "roomId": "string",
    "seq": "int64",
    "timestamp": "int64",
    "type": "string"
  },
  "Discovery": {
    "roomId": "string",
    "hostName": "string",
    "hostIp": "string",
    "port": "int",
    "playersCount": "int",
    "maxPlayers": "int",
    "gameMode": "string"
  },
  "GameStart": {
    "seed": "int64",
    "dealerSeat": "int",
    "initialHandsSummary": "bytes (optional checksum)"
  },
  "PlayerAction": {
    "playerSeat": "int",
    "action": "string",
    "tile": "int (optional)",
    "extra": "bytes (optional)"
  },
  "Snapshot": {
    "turn": "int",
    "handsEncoded": "bytes (only host visible full hands if needed)",
    "publicState": {
      "discards": "array",
      "melds": "array",
      "scores": "array"
    }
  }
}
```

15. 常见实现决策与权衡建议
---------------------------
- WebSocket (TCP) 优点：开发快速、可靠性高；缺点：略高延迟（局域网差异小）。
- UDP 优点：低延迟，适合实时游戏；缺点：需自建可靠层、顺序控制。
- 权威 Host vs 去中心化：权威实现简单且安全；去中心化需一致性算法（复杂）。
- protobuf 推荐于消息频繁与小数据包场景；JSON 便于调试。

16. 上架与权限（注意）
----------------------
- 鸿蒙上架需符合平台小程序/Ability 的清单与权限要求。
- 应声明网络使用权限、局域网/附近设备权限（具体权限名参见鸿蒙官方文档）。
- 若使用设备标识或打点上报，注意隐私合规与用户同意。

17. 交付物（建议）
------------------
- 可运行的鸿蒙包（安装包）
- 源代码仓库（包含 CI）
- 协议文档（protobuf schema）
- 测试用例与网络仿真脚本
- 操作手册与部署说明
- AI 模块说明（参数、难度调节）

18. 风险与备注
----------------
- Host 断线与主机迁移为高复杂度功能，建议首版不支持或限制（如房主掉线判负或自动托管为 AI）。
- 设备间网络差异（部分路由器可能屏蔽广播）——需要在测试覆盖中重点验证。
- 与鸿蒙平台能力（Nearby / Softbus 等）集成时，需查阅并遵守最新 SDK 文档。

附：快速开发 checklist（精简）
--------------------------------
- [ ] 确定麻将规则集（国标/地方/自定义）
- [ ] 选定 ArkTS / JS / Native（优先 ArkTS）
- [ ] 搭建本地 Socket/Discovery 原型
- [ ] 实现 Host 与客户端基础连接与消息回路
- [ ] 实现规则引擎（发牌、出牌、判胡）
- [ ] 实现基础 AI（规则式）
- [ ] UI 基本流程（房间 -> 准备 -> 对局）
- [ ] 增加重连、快照、心跳机制
- [ ] 测试（真实多设备 + 网络仿真）
- [ ] 优化与上架准备

下一步建议（可选）
------------------
- 我可以把上述文档转换为一个可分配到 Sprint 的任务清单（Jira / GitHub Issues），并为每个任务生成模板与验收标准。
- 或者我可以生成 protobuf schema 与示例通信代码（Host 与 Client 的最小可运行原型），以便快速搭建网络原型。
- 告诉我你希望优先做哪一项：A）生成 protobuf 与网络原型代码，还是 B）生成详尽的任务拆分（含估时）？

如果你现在需要，我可以立刻为你生成：
- 1) protobuf 消息定义草案 + Host/Client Node/TS 的简易通信示例；
- 2) 或者把上述开发计划拆成 2 周 Sprint 的 issue 列表（含验收标准）。

请选择你想要的下一步，或告诉我你使用的具体技术栈（ArkTS / JS / Native）与是否需要把 AI 做成可热插拔模块。